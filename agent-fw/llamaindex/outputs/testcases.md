**Revised Test Cases – Complete, Correct, and Consistent with Requirements**

| ID | Title | Preconditions | Steps | Expected Result | Priority |
|----|-------|----------------|-------|-----------------|----------|
| TC-001 | Successful user registration with valid email & password | No existing account for the email | 1. Navigate to registration page  <br>2. Enter a valid email (e.g., `user@example.com`)  <br>3. Enter a password that meets policy (≥8 chars, at least one letter & one number)  <br>4. Submit | Account is created, a verification email is sent, and a “registration successful” message is shown | High |
| TC-002 | Registration fails with invalid email format | – | 1. Open registration page  <br>2. Enter `invalid-email`  <br>3. Enter a valid password  <br>4. Submit | Error “Invalid email address” is displayed; no account is created | High |
| TC-003 | Registration fails when email contains leading/trailing spaces | – | 1. Open registration page  <br>2. Enter `  user@example.com  `  <br>3. Enter a valid password  <br>4. Submit | Email is trimmed/validated; system rejects with “Invalid email address” (or trims and proceeds – test verifies expected behavior). No account is created if rejected. | Medium |
| TC-004 | Registration fails when password is shorter than 8 characters | – | 1. Open registration page  <br>2. Enter a valid email  <br>3. Enter `Abc12`  <br>4. Submit | Error “Password must be at least 8 characters” is shown; account is not created | High |
| TC-005 | Registration fails when password lacks numeric character | – | 1. Open registration page  <br>2. Enter a valid email  <br>3. Enter `Password`  <br>4. Submit | Error “Password must contain at least one number” is shown; account is not created | High |
| TC-006 | Registration fails when password lacks alphabetic character | – | 1. Open registration page  <br>2. Enter a valid email  <br>3. Enter `12345678`  <br>4. Submit | Error “Password must contain at least one letter” is shown; account is not created | High |
| TC-007 | Registration with email containing plus sign and sub‑domain | – | 1. Open registration page  <br>2. Enter `john+test@sub.example.co.uk`  <br>3. Enter a valid password  <br>4. Submit | Account is created and verification email is sent | Medium |
| TC-008 | Registration with Unicode characters in email (if supported) | – | 1. Open registration page  <br>2. Enter `用户@例子.公司`  <br>3. Enter a valid password  <br>4. Submit | Account is created and verification email is sent (or appropriate error if Unicode not supported) | Medium |
| TC-009 | Registration fails when email already exists and is **verified** | Verified account exists for `duplicate@example.com` | 1. Open registration page  <br>2. Enter `duplicate@example.com` and a valid password  <br>3. Submit | System returns generic error “Unable to create account” (no indication of existing account); no new account is created | High |
| TC-010 | Registration with email already exists but is **unverified** | Unverified account exists for `duplicate@example.com` | 1. Open registration page  <br>2. Enter `duplicate@example.com` and a valid password  <br>3. Submit | System informs that a verification email has already been sent; no new account is created | High |
| TC-011 | Simultaneous registration attempts for the same email from two devices | – | 1. Device A: start registration with `simul@example.com`  <br>2. Device B: start registration with same email  <br>3. Both submit within a few seconds | Only one account is created; the second attempt receives an error indicating the email is already in use (or that a verification email has been sent) | High |
| TC-012 | Verification link works once and becomes invalid after use | Account created, verification email received | 1. Click verification link  <br>2. Observe successful verification message  <br>3. Click the same link again | First click verifies the account; second click shows “Link already used” or “Invalid link” error | High |
| TC-013 | Verification link expires after defined period (assume 24 h) | Verification email received, link not used | 1. Wait 25 hours  <br>2. Click verification link | System shows “Link expired” error; account remains unverified | Medium |
| TC-014 | Verification link reuse after account already verified | Account already verified, link still accessible | 1. Click verification link again | System shows “Account already verified” (or generic invalid link) | Medium |
| TC-015 | Registration flow when email service is down | Email service simulated outage | 1. Complete registration form with valid data  <br>2. Submit | System shows error “Verification email could not be sent”; registration is rolled back (no orphaned record) | High |
| TC-016 | Registration fails on database write error | Database forced to reject insert | 1. Submit registration with valid data  <br>2. Observe response | System returns generic error “Unable to complete registration” and no user record exists | High |
| TC-017 | Rate‑limit enforcement on registration API | More than allowed requests per minute (e.g., 11 requests) | 1. Rapidly send 11 registration requests with different emails  <br>2. Observe responses | First 10 requests are processed (or queued); 11th request returns HTTP 429 “Too Many Requests” | Medium |
| TC-018 | Bot protection – registration blocked for disposable email domain | – | 1. Attempt registration with `temp@mailinator.com`  <br>2. Submit | System rejects registration with message “Disposable email addresses are not allowed” | Medium |
| TC-019 | Successful login with verified account | Account verified, password known | 1. Navigate to login page  <br>2. Enter email and correct password  <br>3. Submit | User is logged in, session cookie is set with HttpOnly & Secure flags, dashboard displayed | High |
| TC-020 | Login fails for unverified account | Account created but not verified | 1. Go to login page  <br>2. Enter correct email & password  <br>3. Submit | Error “Account not verified. Please check your email.”; login attempt is counted toward lockout | High |
| TC-021 | Login fails with incorrect password | Verified account exists | 1. Go to login page  <br>2. Enter correct email, wrong password  <br>3. Submit | Error “Invalid credentials”; attempt is counted toward lockout | High |
| TC-022 | Account lockout after 5 consecutive failed logins | Verified account exists | 1. Perform 5 login attempts with wrong password  <br>2. Observe response after each attempt | After 5th attempt, account is locked and message “Account locked for 10 minutes” is shown | High |
| TC-023 | Login attempt while account is locked | Account locked (10 min remaining) | 1. Attempt login with correct credentials  <br>2. Submit | System returns “Account locked. Try again in X minutes.”; no session created | High |
| TC-024 | Successful login **after** lockout period expires | Account locked for 10 min | 1. Wait 10 min + 5 s  <br>2. Attempt login with correct credentials  <br>3. Submit | Login succeeds; lockout counter resets | High |
| TC-025 | Lockout counter resets after successful login before reaching 5 attempts | Verified account exists | 1. Perform 2 failed login attempts  <br>2. Perform a successful login  <br>3. Perform 3 more failed attempts | After successful login, failure count resets; 5 new failures are required to trigger lockout again | Medium |
| TC-026 | Lockout counter resets automatically after lockout period expires | Account locked for 10 min | 1. Wait 10 min + 5 s  <br>2. Perform a single failed login attempt  <br>3. Observe response | Counter starts from 1; lockout will occur after 5 new failures | Medium |
| TC-027 | Distributed brute‑force login attempts do not bypass lockout | Multiple IPs each sending login attempts | 1. From 5 different IPs, send 1 failed login per IP repeatedly (total >5)  <br>2. Observe lockout behavior | Account is locked after 5 total failed attempts regardless of source IP; subsequent attempts receive lockout message | High |
| TC-028 | Login with password containing leading/trailing whitespace | Verified account with password `Pass1234` | 1. Attempt login with password `"  Pass1234  "` (spaces added)  <br>2. Submit | System trims whitespace and login succeeds (if trimming is defined); otherwise fails – test verifies expected behavior (should trim) | Medium |
| TC-029 | Session expires after 30 minutes of inactivity | User logged in | 1. Remain idle for 31 minutes (no requests)  <br>2. Attempt to navigate to a protected page | User is redirected to login page with “Session expired” message | High |
| TC-030 | Background AJAX calls do **not** keep session alive | User logged in, page performs periodic AJAX every 5 min | 1. Let the page run for 35 minutes  <br>2. Attempt to access a protected page | Session expires (AJAX excluded from activity tracking) and user is redirected to login | Medium |
| TC-031 | Session stays alive with user‑initiated activity before timeout | User logged in | 1. Perform a normal UI action (e.g., click a menu) at minute 25  <br>2. Remain idle until minute 55  <br>3. Access protected page | Session remains active because activity reset the timer; no logout occurs | High |
| TC-032 | Forgot‑Password flow – email delivery success | Verified account exists | 1. Click “Forgot password” link  <br>2. Enter registered email  <br>3. Submit  <br>4. Check inbox (within 5 min) | Reset email arrives, contains a one‑time usable link | High |
| TC-033 | Forgot‑Password email delivery failure (simulated outage) | Email service down | 1. Request password reset  <br>2. Submit | System shows error “Unable to send reset email”; no reset token is generated | High |
| TC-034 | Forgot‑Password link can be used only once | Reset email received | 1. Click reset link, set new password  <br>2. Attempt to reuse same link | Second use fails with “Link already used” or “Invalid link” error | High |
| TC-035 | Forgot‑Password link expires after defined period (assume 24 h) | Reset email received, wait >24 h | 1. Click expired link | System shows “Link expired” error; password not changed | Medium |
| TC-036 | Forgot‑Password request throttling (rate limit) | More than 5 reset requests within 1 minute for same email | 1. Submit 6 password‑reset requests rapidly  <br>2. Observe responses | First 5 requests processed; 6th returns HTTP 429 “Too Many Requests” | Medium |
| TC-037 | Profile view displays correct current data | User logged in | 1. Navigate to “Profile” page | All fields (name, phone, avatar) show the latest saved values | High |
| TC-038 | Successful profile edit – atomic update | User logged in, existing profile data | 1. Change name, phone, and upload a valid avatar (≤2 MB, JPEG/PNG)  <br>2. Click “Save”  <br>3. Observe UI update | All changes are persisted; UI reflects new values instantly; no partial updates | High |
| TC-039 | Profile edit fails when avatar exceeds size limit | User logged in | 1. Attempt to upload a 5 MB image as avatar  <br>2. Click “Save” | Validation error “Avatar exceeds maximum size of 2 MB”; no changes are saved | High |
| TC-040 | Profile edit fails with unsupported avatar format | User logged in | 1. Upload a `.gif` or `.exe` file as avatar  <br>2. Save | Error “Unsupported file type”; profile remains unchanged | High |
| TC-041 | Profile edit fails when one field is invalid (atomicity) | User logged in, existing profile | 1. Enter an invalid phone number `12345`  <br>2. Change name to “Bob”  <br>3. Click “Save” | Validation error displayed; **none** of the changes are persisted (atomic) | High |
| TC-042 | Phone number validation – accepted international format | User logged in | 1. Enter phone `+1-202-555-0123`  <br>2. Save  <br>3. Verify acceptance | Phone number accepted and stored in normalized form | Medium |
| TC-043 | Phone number validation – rejected malformed format | User logged in | 1. Enter phone `abc-123`  <br>2. Save | Error “Invalid phone number format”; profile unchanged | Medium |
| TC-044 | Profile fields accept special characters and emojis without XSS | User logged in | 1. Set name to `Alice <script>alert(1)</script> 😊`  <br>2. Save | Input is sanitized; stored value displays literal text and emoji; no script execution occurs | High |
| TC-045 | Concurrent profile edits – last‑write wins | User logged in on two browsers | 1. Browser A changes name to “Bob” and saves  <br>2. Immediately, Browser B changes name to “Carol” and saves  <br>3. Refresh both browsers | Final stored name is “Carol”; both sessions show the same final value; no data corruption | Medium |
| TC-046 | Unauthorized profile edit via CSRF token missing | Attacker crafts POST to `/profile/update` without valid CSRF token | 1. Send request with valid session cookie but no/invalid CSRF token | Server returns 403 “Invalid CSRF token”; profile unchanged | High |
| TC-047 | Upload of malicious file disguised as avatar is blocked | User logged in | 1. Rename a web‑shell script to `avatar.jpg` and attempt upload  <br>2. Save | System detects MIME mismatch or scans content and rejects upload; returns security warning | High |
| TC-048 | Session identifier is HttpOnly and Secure | User logged in over HTTPS | 1. Inspect session cookie via browser dev tools | Cookie has `HttpOnly` and `Secure` flags; not accessible via JavaScript | High |
| TC-049 | Session store outage forces logout of active sessions | Session store (e.g., Redis) stopped | 1. User with active session performs any request  <br>2. Observe response | System returns 401/403 and redirects to login page; user is logged out | High |
| TC-050 | Password hashing algorithm migration does not break login | Existing users have bcrypt hashes; new users use Argon2 | 1. Login with a bcrypt‑hashed password  <br>2. Login with an Argon2‑hashed password | Both logins succeed; system correctly detects hash identifier | Medium |
| TC-051 | Password stored in plaintext is detected (security test) | Simulate misconfiguration causing plaintext storage | 1. Register a new user  <br>2. Inspect database record | Password field contains a hash, not the raw password; test fails if plaintext is found | High |
| TC-052 | TLS enforced for all authentication endpoints | Attempt HTTP (non‑TLS) request to `/login` | 1. Send request over HTTP | Server redirects to HTTPS or rejects with error; no credentials transmitted over plain text | High |
| TC-053 | Email enumeration via login error messages is prevented | – | 1. Attempt login with non‑existent email  <br>2. Observe error message | Generic error “Invalid credentials” is shown (no indication of email existence) | High |
| TC-054 | Email enumeration via registration error messages is prevented | – | 1. Attempt registration with an already‑registered email  <br>2. Observe error | Generic message “Unable to create account” (or “Verification email sent if account does not exist”) is displayed | High |
| TC-055 | Performance – registration completes within 2 seconds under normal load | Load test with 100 concurrent registrations | 1. Measure response time for each request | ≥95 % of registration responses are ≤2 seconds | High |
| TC-056 | Performance – login completes within 2 seconds under normal load | 200 concurrent login attempts with valid credentials | 1. Measure response time | ≥95 % of login responses are ≤2 seconds | High |
| TC-057 | Performance – profile update completes within 2 seconds under normal load | 150 concurrent profile edit requests | 1. Measure response time | ≥95 % of updates are ≤2 seconds | High |
| TC-058 | Email delivery reliability – verification email arrives within 5 minutes 99 % of the time | Simulated normal email service | 1. Register 100 users  <br>2. Measure time until each verification email is received | ≥99 % of emails arrive within 5 minutes | High |
| TC-059 | Email delivery reliability – password‑reset email arrives within 5 minutes 99 % of the time | Simulated normal email service | 1. Request password reset for 100 users  <br>2. Measure delivery time | ≥99 % of reset emails arrive within 5 minutes | High |
| TC-060 | Session token revocation after password change | User logged in, then changes password via profile edit | 1. Change password  <br>2. Attempt to use old session cookie on another browser | Old session is invalidated; user is forced to re‑authenticate | High |
| TC-061 | Concurrency limit on simultaneous sessions per user (assume max 3) | User already has 3 active sessions | 1. Attempt login from a fourth device  <br>2. Observe response | System denies new session with message “Maximum concurrent sessions reached” (or logs out oldest session, depending on spec) | Medium |
| TC-062 | Registration with missing required fields (email or password) | – | 1. Submit registration form leaving email blank  <br>2. Submit again leaving password blank | Appropriate validation errors (“Email is required”, “Password is required”) are shown; no account created | High |
| TC-063 | Registration with whitespace‑only email or password | – | 1. Enter spaces only in email field  <br>2. Enter spaces only in password field  <br>3. Submit | System trims and validates; treats as empty → shows required‑field errors; no account created | Medium |
| TC-064 | Login attempt with correct credentials but account is **disabled** (admin action) | Account exists and is marked disabled | 1. Attempt login with correct email/password  <br>2. Submit | Error “Account disabled. Contact support.”; no lockout counting | High |
| TC-065 | Forgot‑Password request for **unverified** account | Unverified account exists | 1. Request password reset  <br>2. Submit | System sends reset email (if allowed) or returns generic message “If the account exists, a reset link has been sent” – test verifies that no information leakage occurs | Medium |
| TC-066 | Session expiration after long inactivity (90 days) triggers token revocation | User logged in, then inactive for >90 days | 1. Attempt to use existing session cookie after 90 days | System forces re‑authentication; old session is invalidated | Medium |
| TC-067 | CSRF protection on login endpoint | Attacker crafts POST to `/login` without CSRF token (if CSRF is required) | 1. Send login request without CSRF token | Server either ignores CSRF for login (common) or returns 403; test verifies expected behavior per spec | Low |
| TC-068 | Password reset link contains a cryptographically strong token (≥128‑bit) | – | 1. Inspect reset link URL after request | Token length meets security requirement; no predictable patterns | Medium |
| TC-069 | Verification link contains a cryptographically strong token (≥128‑bit) | – | 1. Inspect verification link URL after registration | Token length meets security requirement; no predictable patterns | Medium |
| TC-070 | Audit log entry created for successful login | Successful login performed | 1. Perform login  <br>2. Query audit log | Log records user ID, timestamp, IP, and outcome “success” | Medium |
| TC-071 | Audit log entry created for failed login attempt | Failed login performed | 1. Perform login with wrong password  <br>2. Query audit log | Log records user ID (if known), timestamp, IP, and outcome “failure” | Medium |
| TC-072 | Account lockout event is logged | Account becomes locked after 5 failures | 1. Trigger lockout  <br>2. Query audit log | Log records user ID, timestamp, lockout duration, and reason “exceeded failed login attempts” | Medium |

*Priorities:* **High** – critical for security or core functionality; **Medium** – important for reliability, security, or performance; **Low** – optional or low‑risk scenarios.